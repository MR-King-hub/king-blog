# 服务端口
PORT=3001

# OpenAI API Key（用于 LangChain 调用 GPT）
OPENAI_API_KEY=sk-xxxx

# 数据存储目录
DATA_DIR=./data

# JWT 密钥（用于签名 token）
# ⚠️ 生产环境必须设置一个强密钥！可以用这个命令生成：
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-me
